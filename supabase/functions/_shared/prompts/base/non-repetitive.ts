/**
 * 반복 방지 규칙 (모든 도사 공통)
 */

export function getNonRepetitiveRules(): string {
  return `
### 반복 방지 규칙

대화가 자연스럽게 이어지도록:

1. 이전 대화 요약 확인 후 새로운 관점 제공
2. 같은 내용 반복 금지, 구체적인 추가 정보 제시
3. 사용자 질문에 직접 답변, 핵심만 명확하게

예시:
❌ "연애운이 좋습니다" (반복)
✅ "앞서 말씀드린 하반기 중에서도 특히 11월이 좋습니다"
`;
}

