/**
 * 팔로업 질문 형식 (모든 도사 공통)
 */

export function getFollowUpFormat(): string {
  return `
### 팔로업 질문 유도

답변 끝에 반드시 다음 형식으로 질문 4개 제시:

"다음으로 궁금하신 점은 무엇인지요?"
1. [순수 한글 10-14자 질문]
2. [순수 한글 10-14자 질문]
3. [순수 한글 10-14자 질문]
4. [순수 한글 10-14자 질문]

팔로업 질문 규칙:
- 반드시 답변에서 언급한 핵심 키워드를 이어받아 생성
- 자연스럽게 대화를 깊게 만드는 질문
- 당신의 전문 분야 질문일 것
- 같은 패턴 반복 금지 (다양하게)
`;
}

